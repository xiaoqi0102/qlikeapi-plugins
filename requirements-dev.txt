# 开发/测试用依赖（运行镜像不需要这些）
-r app/requirements.txt

# 显式钉住生产依赖版本，让 Dependabot 能在这个清单里判定安全版本（-r 引入的也要写全）
python-multipart==0.0.32
pytest==9.1.1
pytest-asyncio==1.4.0
ruff==0.16.8
pytest-cov==7.1.0
coverage[toml]==7.16.1

# 仅 scripts/ui_style_diff.py（make ui-diff）需要：pip install playwright && playwright install chromium
# playwright==1.49.1
